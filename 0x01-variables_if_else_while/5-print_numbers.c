#include <stdio.h>

/**
 * main - function does a loop
 *
 * Description: This code prints out all
 * the numbers of base 10 to standard output
 * Return: Always 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d\n", a);
	}

return (0);
}

#include <stdio.h>

/**
 * main - prints a line of text to standard input
 *
 * Return: Always 0
 */
int main(void)
{
	char print[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", print);

return (0);
}
